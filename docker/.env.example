# Required: encryption key for credentials at rest.
# Generate with: openssl rand -base64 32
KEYRING_PASSWORD=

# Optional
TZ=UTC
DASHBOARD_PORT=4242

# Host path mounted into container as /data/kopia-repository
# Example: /srv/docker/kopia/data/repository
SYNC_DIR_KOPIA=./data/kopia-repository
